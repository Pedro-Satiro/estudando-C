#include <stdio.h>
#include <stdlib.h>

int main(){

    //inteiro %d float %f  char é afabeticos %c
    // boleanos (false=0 e true=1) precisa inportar da biblioteca stdbool.h %d
    // em float para colocar a quantidade de casas decimais é %.2(quantidade)f = %.2f

    // soma



    int a = 10, b ;

    printf("\n ola Pika! ");
    printf("o valor da variavel a e = %d", a);
    scanf("%d", &b);
    printf("ja o valor da variavel b e = %d", b);
   // printf("\n %d",a);

    system ("exit");
    return 0;
}